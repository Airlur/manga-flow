# Task 清单（MangaFlow）

> 任务执行时同步勾选。新增任务请追加在最新版本下。

版本记录
- v0.7.3 (2026-03-27)：设置面板侧边栏 / 顶部标签切换；OpenAI 兼容多 provider 管理、自动拉取模型与手动补模落地。
- v0.7.2 (2026-03-26)：Popup / 设置面板局部 React 化；统一字体 token、自定义控件与站点策略设置 UI。
- v0.7.1 (2026-03-26)：贴边悬浮球交互收敛；补充 popup 恢复入口、原/译切换、阶段耗时与默认预检显示策略。
- v0.7 (2026-03-23)：承接旧版本未完成项，新增 UI 重构、Provider 抽象、PaddleOCR、本地服务、LaMa、站点策略与 WebDAV 计划。
- v0.6.1 (2026-02-05)：渲染决策稳定化（短文本不擦除、气泡优先擦除）、气泡分析范围扩展、设置面板生效。
- v0.5 (2026-02-03)：语义优先渲染策略与遮罩/字号统一收敛。
- v0.4 (2026-02-02)：修复短词误过滤、特殊气泡识别不足与组内保护。
- v0.3 (2026-02-02)：ROI/缓存/日志/擦除优先级收敛与体验优化。
- v0.2 (2026-01-31)：OCR 裁剪识别与气泡级聚类改造。
- v0.1 (2026-01-31)：阶段三修复与擦除效果优化。

---

## v0.1 任务列表

### P0（必须先做）
- [ ] 文本块聚类：将同气泡/同段落行块合并为组（输出 group bbox）
- [ ] 组级 mask 生成 + dilation（确保完全覆盖原文笔画）
- [ ] inpaint 集成与调用（优先 Telea / inpaint.js），失败则 fallback
- [ ] 渲染逻辑改为“按组排版”，避免行级叠字

### P1（优化）
- [ ] OCR ROI 裁剪策略（仅对候选区域调用 OCR）
- [ ] 水印/广告关键词过滤（站点级可配置）
- [ ] 位置过滤：边角/底部固定区域降权

### P2（可延后）
- [ ] 可配置“mask 扩展倍率/擦除强度”开关
- [ ] 调试红框默认关闭 + 快捷开关
- [ ] 低分辨率预处理开关（提升移动端性能）

---

## 备注
- 每次改动后更新完成状态。
- 如 plan.md 更新版本，请在此增加新版本分组。

---

## v0.2 任务列表

### P0（必须先做）
- [x] OCR 裁剪识别：按候选 ROI 裁剪后调用 OCR（支持批量请求）
- [x] 文本块聚类：将同气泡/同段落行块合并为组（输出 group bbox）
- [x] 组级 mask + dilation（覆盖完整原文）
- [x] 组级渲染：译文按组排版，避免行级叠字

### P1（优化）
- [x] 轻量候选 ROI 检测（无模型版本：连通域/阈值法）
- [ ] 水印/广告关键词过滤（站点级可配置）
- [ ] OCR 结果二次修正：行间距、重叠率、相似文本去重
- [x] 开发模式开关（默认开启）：阶段选择 + 红框显示独立开关
  - 阶段 A：仅 ROI 裁剪检测（红框），**不调用 OCR**
  - 阶段 B：ROI + OCR（只识别裁剪区域）
  - 阶段 C：OCR + 翻译（不渲染）
  - 阶段 D：完整流程（擦除 + 渲染）
- [x] 缓存命中日志：OCR/翻译命中时在控制台明确标记

### P2（可延后）
- [ ] per-block 交互编辑（拖拽、缩放、透明度）
- [ ] 文本/遮罩样式设定（用户可选）

---

## v0.3 任务列表

### P0（必须先做）
- [x] ROI 策略调整：避免整页大框（必要时可临时关闭 ROI）
- [x] OCR 文本块日志输出（缓存命中与否都输出）
- [x] 缓存命中仍绘制 OCR 红框
- [x] 修复“清除缓存”按钮：清除 `ocr-cache` 与 `translation-cache`

### P1（重要优化）
- [x] 水印/广告关键词过滤（站点黑名单 + 关键词）
- [ ] OCR 文本块拆分/聚类修正（行距/高度/位置）
- [x] 组级 mask 扩边与合并策略加强

### P2（可延后）
- [ ] 自动翻译新加载图片（可选开关，默认关闭）
- [ ] 翻译节流策略（按可见图数量）
- [x] 可视化诊断开关（ROI/OCR/分组层级）
- [ ] per-block 交互编辑（拖拽、缩放、透明度）
- [ ] 文本/遮罩样式设定（用户可选）

---

## v0.4 任务列表

### P0（必须先做）
- [x] 拟声词规则改造：移除“1~4 韩文字=拟声词”规则，改为重复模式/词典
- [x] 短词/小块过滤阈值修正：放宽 length 与 area 的过滤阈值
- [x] 组内保护策略：同气泡组内保留短词（但水印/URL 强过滤）

### P1（重要优化）
- [x] 特殊气泡 OCR 增强：ROI 内二次预处理（灰度/对比度/放大/反色）
- [x] 短词渲染盒子扩展：缓解字体偏小问题
- [ ] 水印+正文混块拆分修正：减少误删
- [ ] 过滤与遮罩解耦策略：过滤块不翻译但可选遮罩（可开关）

### P2（可延后）
- [ ] 自动翻译新加载图片（可选开关，默认关闭）
- [ ] 翻译节流策略（按可见图数量）
- [ ] per-block 交互编辑（拖拽、缩放、透明度）
- [ ] 文本/遮罩样式设定（用户可选）

---

## v0.5 任务列表

### P0（必须先做）
- [x] 翻译分组与渲染/擦除分离：翻译按组，渲染按行块（已实现，待验证）
- [x] 组内字号统一：按最长行宽度约束统一字号（已实现，待验证）
- [x] 遮罩宽度自适应：译文宽度 + padding + 上限阈值（已实现，待验证）
- [x] 拟声词 hard drop：不翻译/不擦除/不渲染（已实现）
- [ ] 气泡优先擦除规则：语义分类优先于复杂度判定

### P1（重要优化）
- [ ] ID/昵称识别策略：短文本 + 位置 + 重复性
- [ ] 拟声词/装饰字视觉规则：位置 + 字体大小 + 描边特征
- [ ] 气泡类遮罩跟随气泡/文本区域（不跟译文长度）

### P2（可延后）
- [ ] 自动翻译新加载图片（可选开关，默认关闭）
- [ ] 翻译节流策略（按可见图数量）
- [ ] per-block 交互编辑（拖拽、缩放、透明度）
- [ ] 文本/遮罩样式设定（用户可选）
- [ ] 轻量气泡检测（非模型）

---

## v0.6 任务列表

### P0（必须先做）
- [x] 组级擦除/渲染：擦除与排版按组 bbox 统一处理
- [x] 气泡启发式判定接入：色彩集中度 + 边缘密度（保守阈值）
- [x] 拟声词 hard drop 强化：短词 + 大面积 + 孤立块过滤

### P1（重要优化）
- [ ] 气泡判定阈值调参：降低误判率，允许漏判
- [ ] 遮罩宽度/字号稳定性验证与修正
- [ ] ID/昵称识别策略：短标签/姓名保留或音译

### P2（可延后）
- [ ] 自动翻译新加载图片（可选开关，默认关闭）
- [ ] 翻译节流策略（按可见图数量）
- [ ] per-block 交互编辑（拖拽、缩放、透明度）
- [ ] 文本/遮罩样式设定（用户可选）
- [ ] 轻量气泡检测（非模型）

---

## v0.6.1 任务列表

### P0（必须先做）
- [x] 渲染决策稳定化：短文本不擦除、气泡优先擦除、复杂背景保守遮罩
- [x] 气泡分析范围扩展：统计框放大，降低文字笔画干扰

### P1（重要优化）
- [ ] 设置面板显示设置未生效：字体倍率/颜色/遮罩透明度调整后渲染仍无变化

---

## v0.7.0 任务列表（架构基线与文档承接）

### P0（必须先做）
- [ ] 修正 `pnpm-workspace.yaml`：显式排除后续 `server/` 目录
- [ ] 抽象 Provider 结构：OCR / Inpaint / Translate 三类 provider 注册与切换
- [ ] 为 Google OCR 增加 `DOCUMENT_TEXT_DETECTION / fullTextAnnotation` 对照分支
- [ ] 建立韩漫测试样本集与对比指标（漏字率 / bbox 贴合度 / 拟声词误检 / 单张耗时）
- [ ] 补充统一阶段耗时日志：ROI / OCR / 翻译 / 擦除 / 渲染 / 总耗时
- [x] 修复无漫画图时点击翻译无反馈的问题，必须 toast 提示
- [x] 修复当前 `tsc --noEmit` 的类型报错（`src/content/core/translator.ts`）

### P1（重要优化）
- [ ] 承接旧项：气泡判定阈值调参
- [ ] 承接旧项：遮罩宽度/字号稳定性回归验证
- [ ] 承接旧项：ID/昵称识别策略
- [ ] 承接旧项：水印+正文混块拆分修正
- [ ] 承接旧项：过滤与遮罩解耦策略

### P2（可延后）
- [x] 构建产物路径收敛（新增 `dist-extension/`，可独立加载）
- [ ] host 权限与内容脚本注入范围收敛
- [ ] 站点适配配置数据化

---

## v0.7.1 任务列表（贴边悬浮球交互收敛）

> 已完成：这一阶段只做悬浮球与 popup 最小入口，不扩到整套面板视觉重构。

### P0（必须先做）
- [x] 悬浮球改为右侧贴边伸出式 UI
- [x] 支持沿右边缘上下拖拽，禁止脱离右边
- [x] 增加左上角半透明 `X`，并改为 hover 悬浮球控件时才显示
- [x] 增加动态耗时显示胶囊
- [x] 保留页数黑胶囊并调整到新布局
- [x] 无漫画图时点击翻译立即 toast 提示
- [x] popup 增加“重新显示悬浮球”入口
- [x] 原文 / 译文切换改为延后显示，并在 `...` 右侧出现
- [x] 默认接入 `sitePolicy = auto_detect`，页面通过漫画图片预检后再显示悬浮球
- [x] Toast 增加阶段耗时摘要

### P1（重要优化）
- [x] 增加运行态缓存：原图 / 译图 / 当前显示模式
- [x] 新增消息：`RESTORE_FLOATING_BALL` / `HIDE_FLOATING_BALL` / `TOGGLE_VIEW_MODE`
- [x] 点击 `X` 增加“禁用此网站 / 全局禁用”确认浮层

### P2（可延后）
- [ ] 关闭悬浮球后的状态提示与恢复文案进一步优化
- [ ] 悬浮球展开区的更多操作（如站点加入白名单）延后补充

---

## v0.7.2 任务列表（Popup / Settings 面板视觉重构）

> 这一阶段承接原先误放在 v0.7.1 的 UI 任务，继续按 shadcn-ui / Vercel 黑白灰极简风格推进。

### P0（必须先做）
- [x] 重构 Popup 主面板：语言同一行、统一控件高度、去除原生控件突兀感
- [x] 重构设置面板：分区、标题字号、专业图标、现代输入组件样式
- [x] 修复 Popup / 设置面板 / 悬浮球 / 内容脚本浮层的字体不一致问题，统一字体继承、字重与行高
- [x] 排查并收口宿主页面 CSS 对内容脚本 UI 的字体污染；必要时评估更强的样式隔离方案（已完成字体继承与样式收口，Shadow DOM 延后评估）
- [x] 用 Lucide / 品牌占位 glyph 替换 emoji 图标
- [x] API Key 显隐按钮改为专业 icon（eye / eye-off）
- [x] 完善微软 / DeepL / DeepLX / OpenAI 兼容 API 的表单与 helper text
- [x] API Base URL 下方增加 `/v1/chat/completions` 预览提示
- [x] 将 OpenAI 兼容 API 抽象升级为多提供商结构（完整管理 UI 在 v0.7.3 收口）
- [x] 模型选择器第一版重做：获取列表、手输、统一样式合并（进一步的 provider 级管理延后）
- [x] 新增 `dist-extension/` 最小扩展加载目录，避免继续直接加载整个项目根目录

### P1（重要优化）
- [x] 加入站点运行策略设置（所有站点 / 白名单站点）
- [ ] 设计移动端悬浮球 fallback：不依赖 hover，优先评估 2 段式 `logo + ...` 收缩方案
- [ ] 将移动端关闭 / 原图译图切换 / 禁用站点操作收进 `...` 操作菜单，避免误触开始翻译
- [ ] 评估桌面端是否也统一为更收缩的悬浮球结构，降低阅读遮挡
- [x] 表单错误态 / 成功态 / loading 态统一设计
- [ ] 暗色主题适配
- [ ] 响应式和小屏细节优化

---

## v0.7.3 任务列表（设置面板侧边栏 + OpenAI 多 Provider）

### P0（必须先做）
- [x] 设置面板改为桌面端左侧栏导航、移动端顶部横向 tab
- [x] `常规` tab 顺序对齐 Popup：原文语言 / 目标语言 / OCR / 翻译服务
- [x] `翻译` tab 接入 OpenAI 兼容多 provider 管理
- [x] 支持 provider 新增、删除、启停、优先级调整
- [x] 填写 Base URL + API Key 后自动请求模型列表
- [x] 支持手动添加模型、设置默认模型、删除模型
- [x] background / translator 按启用 provider 顺序回退调用
- [x] 修复本轮新增 UI 文案乱码，统一到 UTF-8 正常文本
- [x] 统一 OCR / provider 图标资源目录，修复 popup / content 中 OCR 图标路径错误

### P1（重要优化）
- [ ] provider 优先级改为真正拖拽排序（当前先用上移 / 下移按钮）
- [ ] 将 OCR 本地 `server/` 服务地址与健康检查正式接入设置面板
- [ ] 小屏细节继续打磨（尤其 provider 面板在手机端的压缩布局）

### P2（可延后）
- [ ] provider 维度的失败次数 / 延迟统计展示
- [ ] 为不同 provider 增加更细的标签与分组（如官方 / 自建 / 中转）

---

## v0.8.0 ?????PaddleOCR ?????

> ????????? UI ?????????? **2 ??????** ???

### P0??????
- [x] ?? `server/` ??????
- [x] ?? `/health`?`/ocr`?`/ocr/batch` ??
- [x] ???? OCR provider?`paddle-local`
- [x] ?????? 2 ?????`PP-OCRv5_mobile_det + korean_PP-OCRv5_mobile_rec`
- [x] ????? `model_dir`?????? D ?
- [x] ???????????????
- [x] ?????? PaddleOCR ???????????
- [x] OCR ????????? `/ocr/json`


